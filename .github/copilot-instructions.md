# Copilot Instructions

- Keep responses concise.
- Work only in this repository unless asked otherwise.
- Prefer minimal JSON and schema edits.
- Preserve manifest/index compatibility and versioned artifact layout.
- Avoid reformatting unrelated registry entries.
- Validate touched data with `python tools/validate_registry.py` when possible.
- Update release docs only when process changes.
